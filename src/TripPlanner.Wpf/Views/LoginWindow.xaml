<Window x:Class="TripPlanner.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign in" Height="280" Width="420">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Email" Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" MinWidth="220"/>

        <TextBlock Text="Password" Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" MinWidth="220"/>

        <TextBlock Text="{Binding Error}" Foreground="Crimson"
                   Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RegisterButton" Content="Register" Margin="0,0,8,0" Click="OnRegister"/>
            <Button Content="Cancel"  Margin="0,0,8,0" IsCancel="True"/>
            <Button x:Name="SignInButton" Content="Sign in" IsDefault="True" Click="OnSignInClick"/>
        </StackPanel>

        <!-- Working overlay -->
        <Border Grid.RowSpan="4" Grid.ColumnSpan="2" Background="#80FFFFFF" x:Name="WorkingOverlay" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,12"/>
                <TextBlock x:Name="WorkingText" Text="Processing..." HorizontalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>