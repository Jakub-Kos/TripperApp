<Window x:Class="TripPlanner.Wpf.Views.DestinationEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Destination" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        MinWidth="520">
  <Window.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
  </Window.Resources>

  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="0,0,0,10">
      <TextBlock Text="Title" FontWeight="SemiBold"/>
      <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="480"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Margin="0,0,0,10">
      <TextBlock Text="Image URL (optional)" FontWeight="SemiBold"/>
      <TextBox Text="{Binding ImageUrl, UpdateSourceTrigger=PropertyChanged}" Width="480"/>
      <TextBlock Text="(Use the Images endpoint for file uploads if needed.)" Opacity="0.6" FontSize="11"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Margin="0,0,0,10">
      <TextBlock Text="Description" FontWeight="SemiBold"/>
      <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Width="480" Height="140"/>
    </StackPanel>

    <DockPanel Grid.Row="3">
      <TextBlock DockPanel.Dock="Left" Text="{Binding Status}" Foreground="Gray" VerticalAlignment="Center"/>
      <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
        <Button Content="Delete" Visibility="{Binding IsEdit, Converter={StaticResource BoolToVisibilityConverter}}"
                Command="{Binding DeleteCommand}" Margin="0,0,8,0"/>
        <Button Content="Save" Command="{Binding SaveCommand}" Margin="0,0,8,0"/>
        <Button Content="Cancel" Command="{Binding CancelCommand}"/>
      </StackPanel>
    </DockPanel>
  </Grid>
</Window>
