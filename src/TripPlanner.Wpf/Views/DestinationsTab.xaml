<UserControl x:Class="TripPlanner.Wpf.Views.DestinationsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Assumes your DestinationsViewModel exposes:
       Items (collection of DestinationRow { DestinationId, Title, Description, Votes, IsChosen, Images }),
       NewTitle, NewDescription,
       ProposeCommand(),
       VoteSelfCommand(row), VoteProxyCommand(row) uses VM.ProxyParticipantId,
       ChooseCommand(row), DeleteCommand(row),
       UploadImageCommand(row).
  -->
  <DockPanel Margin="8">
    <DockPanel.Resources>
      <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </DockPanel.Resources>
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBox Width="220" Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
      <TextBox Width="300" Text="{Binding NewDescription, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
      <Button Content="Propose" Command="{Binding ProposeCommand}"/>
      <Separator Margin="12,0"/>
      <TextBlock Text="Proxy PID:" VerticalAlignment="Center" Margin="12,0,8,0"/>
      <TextBox Width="180" Text="{Binding ProxyParticipantId, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>

    <ItemsControl ItemsSource="{Binding Items}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border BorderBrush="#DDD" BorderThickness="1" Padding="8" Margin="0,0,0,8">
            <DockPanel>
              <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Vote" Margin="8,0,0,0"
                        Command="{Binding DataContext.VoteSelfCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"/>
                <Button Content="Proxy vote" Margin="8,0,0,0"
                        Command="{Binding DataContext.VoteProxyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"/>
                <Button Content="Choose" Margin="8,0,0,0"
                        Command="{Binding DataContext.ChooseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"/>
                <Button Content="Delete" Margin="8,0,0,0"
                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"/>
                <Button Content="Upload image…" Margin="8,0,0,0"
                        Command="{Binding DataContext.UploadImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}"/>
              </StackPanel>

              <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="Gray"/>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                  <TextBlock Text="Votes:" Foreground="Gray" Margin="0,0,4,0"/>
                  <TextBlock Text="{Binding Votes}" Foreground="Gray"/>
                  <TextBlock Text="  " />
                  <TextBlock Text="Chosen" Foreground="SeaGreen"
                             Visibility="{Binding IsChosen, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>

                <!-- Images preview (if you already fetch URLs/thumbnails) -->
                <ItemsControl ItemsSource="{Binding Images}" Margin="0,6,0,0">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border BorderBrush="#EEE" BorderThickness="1" Margin="0,0,6,6">
                        <Image Source="{Binding Url}" Width="120" Height="80" Stretch="UniformToFill"/>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </DockPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </DockPanel>
</UserControl>
